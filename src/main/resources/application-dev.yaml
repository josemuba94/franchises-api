spring:
  datasource:
    url: jdbc:h2:mem:franchise
    driverClassName: org.h2.Driver
    username: josemuba
    password:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create
      show-sql: true
  h2:
    console:
      enabled: true
